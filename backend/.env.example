# FastAPI Backend Environment Variables

# Backend Configuration
PORT=8000
HOST=0.0.0.0

# Database
MONGODB_URL=MONGO DB URL 
# OR for MongoDB Atlas (Production):
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/assignment-solver?retryWrites=true&w=majority

# Security
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000
# For production, add your frontend domain: https://your-frontend-domain.com

# Google OAuth (for Google Classroom integration)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CREDENTIALS_FILE=./credentials.json
GOOGLE_TOKEN_FILE=./token.json

# Primary LLM Provider - Google Gemini
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp
# Get free API key: https://aistudio.google.com/apikey

# Fallback LLM Provider 1 - Groq (Recommended, Very Fast & Free)
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.1-70b-versatile
# Get free API key: https://console.groq.com/keys
# Free tier: 14,400 requests/day, 30 requests/minute

# Fallback LLM Provider 2 - Together.ai (Optional)
TOGETHER_API_KEY=your-together-api-key-here
TOGETHER_MODEL=meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo
# Get free credit: https://api.together.xyz/signup

# Fallback LLM Provider 3 - HuggingFace (Optional)
HF_API_TOKEN=your-huggingface-token-here
HF_MODEL=mistralai/Mistral-7B-Instruct-v0.2
# Get token: https://huggingface.co/settings/tokens

# LLM Configuration
LLM_PROVIDER_PRIORITY=gemini,groq
# Comma-separated list: gemini,groq,together,huggingface
# Providers will be tried in this order

# Duplicate Detection
SIMILARITY_THRESHOLD=0.85
# Range: 0.0 to 1.0 (higher = stricter matching)
ENABLE_DUPLICATE_DETECTION=true

# Subject Auto-Detection
AUTO_DETECT_SUBJECT=true
# If true, will attempt to auto-detect subject from assignment description

# Rate Limiting
RATE_LIMIT_ENABLED=true
# Set to false to disable rate limiting (not recommended for production)

# Redis (optional, for distributed rate limiting)
# REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=agent.log

# Scheduling
SYNC_SCHEDULE_CRON=0 8 * * *
# Daily at 8 AM (cron format)

# Authentication
AUTH_RETRY_ATTEMPTS=3
AUTH_TIMEOUT_SECONDS=300

# File Storage
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Environment
ENVIRONMENT=development
# Change to 'production' when deploying
