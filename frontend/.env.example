# Next.js Frontend Environment Variables

# Backend API URL (publicly exposed to client)
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000
# For production, use your deployed backend URL:
# NEXT_PUBLIC_BACKEND_API_URL=https://your-backend-domain.com

# Backend API URL for server-side (NextAuth callbacks - NOT exposed to client)
BACKEND_API_URL=http://localhost:8000
# For production, MUST be set to your deployed backend URL:
# BACKEND_API_URL=https://your-backend-domain.com

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here-minimum-32-characters-long-random-string
# Generate with: openssl rand -base64 32
# For production NextAuth URL: https://your-frontend-domain.com

# Google OAuth (for NextAuth + Google Classroom)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Get from: https://console.cloud.google.com/apis/credentials
# Make sure to enable Google Classroom API

# Environment
NODE_ENV=development
# Change to 'production' when deploying

# Optional: Analytics (if needed)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-analytics-id